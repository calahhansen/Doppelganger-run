<nav class="navbar navbar-expand-lg navbar-dark bg-success">
                <a class="navbar-brand">Doppelganger</a>
                <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
                  <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
                  <div class="navbar-nav">
                    <a class="nav-item nav-link active" href="/home">Home <span class="sr-only">(current)</span></a>
                    <a class="nav-item nav-link" href="/profile">Profile</a>
                    <a class="nav-item nav-link" href="/logIn">Log Out</a>
                  </div>
                </div>
              </nav>

<div id="home">
  <div class="container">
    <div class="row">
      <div class="col-8 offset-2">
        <h2>Submit a Task!</h2>
        <form class="clearfix mb-4" action="POST">
          <div class="form-group">
            <label for="example-text">Title</label>
            <input type="text" id="example-text" class="form-control" aria-describedby="example-text" placeholder="Enter Title">
          </div>
          <div class="form-group">
            <label for="example-description">Description</label>
            <textarea class="form-control" id="example-description" aria-describedby="example-description" placeholder="Enter Description"></textarea>
          </div>
          <label for="example-category">Category</label>
<select class="form-control" id="example-category" aria-describedby="example-category" name="cars">
    <option value="Indoor Task">Indoor Task</option>
    <option value="Outdoor Task">Outdoor Task</option>
    <option value="Errand Run">Errand Run</option>
    <option value="Sell Item">Sell Item</option>
  </select>
  <br></br>
          <button id="submit" class="btn btn-primary float-right">Submit</button>
        </form>


        <h3>Tasks:</h3>
        <ul id="example-list" class="list-group">
          {{#each tasks}}
          <li data-id="{{this.id}}" class="list-group-item">
            <a href="tasks/{{this.id}}">{{this.text}}</a>
            <br>
            <a href="tasks/{{this.id}}">{{this.description}}</a>
            <br>
            <a href="tasks/{{this.id}}">{{this.category}}</a>
            <br>
            <a href="tasks/{{this.id}}">{{this.city}}</a>
            <button class="btn btn-danger float-right delete">ｘ</button>
          </li>
          <br>
          {{/each}}
        </ul>
      </div>
    </div>
  </div>
</div>

<script type="text/javascript" src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.2.1/jquery.min.js"></script>
<script src="/js/index.js"></script>
